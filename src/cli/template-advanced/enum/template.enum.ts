export enum ETemplateType {
  'article' = 1,
  'science' = 2,
  'academic' = 3,
  'policy' = 4,
  'creditRule' = 5,
  'memberLevel' = 6,
}

export enum ETemplateStatus {
  'done' = 1,
  'undone' = 2,
}
